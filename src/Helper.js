'use strict';

/**
 * @class Helper
 */
class Helper {
  /**
   * Create a definition set from a map of objects.
   * @param {Object<string,Object>} objects - The objects.
   * @param {Object} [commonDefinition={}] - The common definition of the objects.
   * @param {Object<string,*>} [specificDefinitions={}] - The specific definitions of the objects.
   * @returns {Object<string,Object>} The service definitions.
   */
  set(objects, commonDefinition = {}, specificDefinitions = {}) {
  }
}

module.exports = Helper;
